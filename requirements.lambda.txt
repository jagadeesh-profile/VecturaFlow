# VecturaFlow — Lambda image runtime deps.
# Subset of requirements.runtime.txt: omits FastAPI/uvicorn/redis/structlog/
# prometheus-client (API-only) but keeps everything the 3 ingestion handlers need.

pydantic==2.12.5
pydantic-settings==2.13.1

openai==1.30.1
# httpx<0.28 — openai 1.30.1 still passes the removed `proxies` kwarg.
httpx==0.27.2

pinecone-client==3.2.2

langchain==0.2.1
langchain-openai==0.1.8
# Lambda Python 3.11 accepts the 0.7.0 manylinux2014 wheel; newer transitive
# releases can fall back to Rust source builds on this runtime.
tiktoken==0.7.0
langchain-community==0.2.1
langchain-text-splitters==0.2.0

# Parsing: PyMuPDF (primary PDF backend, no C-deps beyond mupdf wheel),
# python-docx (DOCX), pandas (CSV). `unstructured` intentionally excluded —
# its partial install segfaulted in the backfill script; PyMuPDF handles all
# production PDFs cleanly.
pymupdf==1.24.5
python-docx==1.1.2
pandas==2.2.2

boto3==1.34.102
botocore==1.34.102

python-json-logger==2.0.7
python-dotenv==1.0.1
